# Car Detection (YOLOv8) - Streamlit App

This folder contains the Streamlit app for car detection.

## Where is the trained model?
Place the trained weights file at:

- `car_app/models/best.pt`

## Run locally
```bash
pip install -r ../requirements.txt
streamlit run car_app/app.py
```

